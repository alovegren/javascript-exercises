/*
Each time `discount` is invoked, the `item` object is mutated such that its `price` property is permanently changed to have a value equal to the returned price. For the method to behave as intended, it should just return the current price minus the discount rather than reassign the price property.
*/